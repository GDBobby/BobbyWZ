#pragma once
namespace MapleLib {
	namespace WzLib {
		enum class WzMapleVersion {
			//need some comments here, wtf is classic. wtf is the zlz
			GMS,
			EMS,
			BMS,
			CLASSIC,
			GENERATE,
			GETFROMZLZ
		};
	}
}